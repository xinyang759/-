# 今日学习心得

## 1.结构体（64~71）

### 1.1结构体的概念

属于用户==自定义的数据类型==

### 1.2结构体的定义和使用

语法：struct 结构体名{结构体列表}；

结构体创建变量的三种方式：

- struct结构体名 变量名
- struct 结构体名 变量名={成员1值，成员2值}
- 定义结构体是顺便创建变量

### 1.3结构体数组

作用：将自定义的结构体放入到数组中方便维护

语法：struct 结构体名 数组名[元素个数] = {  {}，{}，....{}   }

### 1.4结构体指针

作用：通过指针访问结构体中的成员

- 利用箭头->可以通过指针访问结构体属性

### 1.5结构体嵌套结构体

作用：结构体中的成员可以是另一个结构体

例如：每个老师辅导一个学生，一个学生的结构体中，记录一个学生的结构体

### 1.6结构体做函数参数

作用：将结构体作为参数向函数中传递

传递方式有两种：

- 值传递
- 地址传递

### 1.7结构体中const使用场景

作用：用const来防止误操作

## 2.vector容器（31~37）

### 2.1vector基本概念

- 功能：vector数据结构与数组非常相似，也成为单端数组

与普通数组的区别：

- 数组是静态的，但vector可以动态扩展（并不是在原有空间上接一段空间，二十找更大的空间，吧原数据拷贝过去，释放原空间）
- vector的迭代器是支持随机访问的迭代器

### 2.2vector构造函数

功能：

- 给vector容器进行赋值

函数原型：

- `vector& operator=（const vector &vec）;`//重载等号操作符。
- `assign(beg, end); `//将[beg,end)区间中的数据拷贝赋值给本身。
- `assign（n,elem;`//将n个elem拷贝赋值给本身。

### 2.3vector容量和大小

功能描述：

- 对vector容器的大小和容量操作

函数原型：

- empty();//判断是否为空
- capacity（）；//判断容器容量
- size（）；//判断容器大小
- resize（int num）；//重新制定容器长度为num

### 2.4vector插入和删除

功能描述：

- 对vector容器进行插入、删除操作

函数原理：

- `push_back(ele);`//尾部插入
- `pop_back();`//尾部删除
- `insert(const_iterator pos, ele);`//迭代器指向位置pos插入ele
- `insert(const_iterator pos, int count,ele);`//迭代器指向位置pos插入count个元素ele
- `erase(const_iterator pos);`删除迭代器指向元素
- `erase(const_iterator start，const_iterator end);`//删除迭代器从start到end之间的元素
- `clear（）；`//删除容器中所有的元素

### 2.5数组存取

功能描述：

- 对vector中的数据的存取操作

函数原型：

- `at(int,idx);`//返回索引idx所指的数据
- `operator[(size_t n)];`//访问索引n的元素
- `front();`//返回第一个
- `back();`//返回最后一个

### 2.6vector互换容器

功能描述：

- 实现两个容器内元素进行互换

函数原型：

- `swap(vec);`//将vec与本身的元素互换

### 2.7vector预留空间

功能简述：

- 减少vector在动态扩展容量时的扩展次数

函数原型：

- `reserve(int len);`//预留len个元素长度，预留位置不初始化，元素不可访问

## 3STL初识（19~22）

### 3.1诞生及基本概念：

为提升复用性，所建立的数据结构和算法的一套标准

称为标准模板库，广义上可分为：容器，算法，迭代器

