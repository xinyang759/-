# 今日学习心得（1~41）

## 1.编写程序的过程

1. 创建项目
2. 创建文件
3. 编写代码
4. 运行代码

## 2.添加注释

### 2.1.单行注释采用“//”

### 2.2.多行注释采用/**/

## 3.变量

### 3.1作用：

给定一段空间命名，便于操作这段空间。

### 3.2语法

数据类型 数据名=初始值；

### 3.3与常量的差别

常量不可变，变量的值可以改变。

### 3.4常量的语法

1. #define 常量名 常量值
2. const 数据类型 常量名=常量值

## 4.关键字

### 4.1作用：

c++中预先保留的单词（标识符），取变量名时不可采用。

### 4.2如下表所示：

![image-20260107225315788](C:\Users\思辰\AppData\Roaming\Typora\typora-user-images\image-20260107225315788.png) 

## 5.标识符

### 5.1作用：

用于给变量和常量取名的一套规则。

### 5.2规则如下：

- 标识符可由数字，字母，下划线组成
- 开头不能是数字
- 标识符不能是关键字
- 标识符区分大小写

## 6.数据类型

### 6.1整型

作用：表示的是整数类型的数据

常见的整型有：

- short(占2个字节)
- int（占4个字节）
- long（Windows中4个字节，可能存在8个字节）
- long long（占8个字节）

### 6.2实型

作用：表示的是小数类型的数据

常见的有：

- float（单精度，占4个字节，7位有效数字）
- double（双精度，占8个字节，15位有效数字）

### 6.3字符型

作用：用于显示单个字符

语法：`chr ch='a';`

- 占用一个字节
- 计算机中储存的是ASCII码值`int(变量名)`可展示

### 6.4字符串类型

作用：用于表示一串字符

语法：

- `chr 变量名[]="字符串值"`
- `sstring 变量名="字符串"`

### 6.5布尔类型

作用：表示真或假的数

只有两个值：

- 真-----ture(本质是1)
- 假-----false（本质是0）

### 6.6数据类型的输入

作用：用于在键盘中获得数据

语法：

`cin>>变量`

### 6.7转义字符

作用：用于表示一些不能显示出来的ASCII字符

现阶段常用的：`\n \t  \\ `

## 7运算符

### 7.1作用：用于执行代码的四则运算

- +：用于表示整数
- -：用于表示负数
- +：加法运算
- -：减法运算
- *：乘法运算
- /：除法运算
- %：取模运算
- ++：前置递增（先加1，再进行运算）
- --：前置递减（先减1，再进行运算）
- ++：后置递增(先运算，再加1)
- --：后置递减（先运算，再减1）

### 7.3赋值运算符

- =：赋值
- +=：加等于
- -=：减等于
- *=：乘等于
- /=：除等于
- %=：取模等于

### 7.4比较运算符

作用：用于比较，并返回一个真值或者假值

- ==：相等于
- ！=：不等于
- `<`：小于
- `>`：大于
- `>=`大于等于
- `<=`小于等于

### 7.5逻辑运算符

作用：根据表达式的值返回真值或者假值

- `!`:非，真变假
- `&&`:与，都真才真，否则为假
- `||`:或，一真为真

## 8.程序流程结构

### 8.1最基本的三种流程结构

- 顺序结构
- 选择结构
- 循环结构

### 8.2选择结构

作用：执行满足条件的语句

语法：

- 单分支if语句：

  `if (条件表达式) {   `

  ` //  代码语句; }`

- 多行if语句：

  `if (条件表达式) {
      // 条件为 true 时执行
      代码语句1;
  } else {
      // 条件为 false 时执行
      代码语句2;
  }`

- 多条件if语句：

  `if (条件表达式1) {
      代码语句1;
  } else if (条件表达式2) { 
      代码语句2;
  // 可添加多个 else if 分支
  } else if (条件表达式n) {
      代码语句n;
  } else {
      // 所有条件都不满足时执行
      代码语句n+1;
  }`

- switch语句：

`switch (表达式) { `

` case 常量值1:        代码语句1;        break; `

` case 常量值2:        代码语句2;        break;` 

`    default:  代码语句n;        break; }`

### 8.3循环结构：

作用：满足循环条件，执行循环语句

语法：

- while语句：

  `while (条件表达式) {  `

  `  // 条件为true时执行的代码块    代码语句;    // 必须有更新循环变量的语句，否则会无限循环`  

  ` }`

- do-while语句：

  `do {
      // 先执行一次的代码块
      代码语句;
  } while (条件表达式); // 注意末尾有分号`

- for语句：

  `for (初始化表达式; 条件表达式; 更新表达式) {  `

  `  // 条件为true时执行的代码块    代码语句;`  

  ` }`

跳转语句：

作用：跳出选择结构或循环结构

- break：立即终止当前循环（跳出循环体）；
- continue：跳过当前循环的剩余代码，直接进入下一次循环；
- goto：跳转到指定标签位置，容易破坏代码结构。