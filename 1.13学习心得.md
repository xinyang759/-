# 1.12学习心得

## 1.快速排序法：

### 1.1步骤：

1. 设置a,low,high，其中a为最左端数字，然后将low和high分别指向最左端和最右端
2. low和high分别朝中间移动，并将指向的数字与a作比，大于a的放右边，小于a的放左边。最后，low和high会重合，将a的值放在该位置上。此时，所有的数字也被该位置分开，成为了两部分
3. 在左右两边继续上述操作。直到分开以后左右两把只有一个数字。

### 1.2函数构造：

- 分区函数：

  ```
    int Partition(vector<int>& a, int low, int high) {
          int pivot = a[low];
          while (low < high) {
              while (low < high && a[high] >= pivot) high--;
              a[low] = a[high];
              while (low < high && a[low] <= pivot) low++;
              a[high] = a[low];
          }
          a[low] = pivot;
          return low;
      }
  ```

- 快排函数：

       `void QuickSort(vector<int>& a, int low, int high) {
        if (low < high) {
            int pivotpos = Partition(a, low, high);
            QuickSort(a, low, pivotpos - 1);
            QuickSort(a, pivotpos + 1, high);
        }
    }`

## 2.并归排序法：

### 2.1步骤：

归并排序基于分治法，核心步骤分为拆分和合并两个阶段，具体流程如下：

一、拆分阶段（分）

1. 确定拆分边界：计算当前数组区间的中间点 mid ，将数组划分为左子区间 [l, mid] 和右子区间 [mid+1, r] 。
2. 递归拆分：对左、右子区间分别递归执行拆分操作，直到每个子区间仅包含1个元素（单个元素天然有序，触发递归终止条件）。

二、合并阶段（合）

1. 初始化指针：设置三个指针， i 指向左子区间起点， j 指向右子区间起点， k 指向临时数组的起始位置。
2. 比较合并：循环比较左、右子区间的当前元素，将较小的元素放入临时数组，并移动对应指针，直到其中一个子区间遍历完毕。
3. 处理剩余元素：将未遍历完的子区间中剩余的元素依次放入临时数组。
4. 写回原数组：将临时数组中的有序元素覆盖回原数组的对应区间，完成一次合并。
5. 逐层合并：从最小的有序子数组开始，逐层向上合并，最终得到完整的有序数组。

### 2.2函数构造：

```
viod mergesort(vector<int> &arr,int l,int r,vector<int> &temp){
   if(l>=r){
   return;
   }
   int mid=(l+r)/2;
   megersort(arr,l,mid,temp);
   mergesort(arr,mid+1,r,temp);
   int i=l,j=mid+1,k=0;
   while(i<=mid&&j<=r){
     if(arr[i]<=arr[j]){
     temp[k++]=arr[i++];
     else temp[k++]=arr[j++];
     }
   }
   while(i<=mid){
   temp[k++]=arr[i++];
   }
   while(i<=mid){
   temp[k++]=arr[j++];
   }
   for(int i=l,j=0;i<=r;i++,j++){
   arr[i]=temp[j]
   }
}
```

## 3.冒泡排序

### 3.1步骤：

从最上面开始，成对比下去，将最大的一个个沉到最底端。

### 3.2函数构造

```
viod bubblesort(int arr[],int l,int r){
   int len=r-l+1;
   for(i=0;i<len-1;i++){
     for(j=0;j<len-1-i;j++){
       if(arr[j]>arr[j+1]){
         swap(arr[j],arr[j+1]);
       }
     }
   }
}
```

