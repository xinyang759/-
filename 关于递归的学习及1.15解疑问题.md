# 1.15学习心得

## 1.递归

### 1.1本质

递归本质就是将问题拆解成更小的相同问题，进而解决。程序中函数会调用自己进行计算。

### 1.2要求

1. 递归必须有终止条件
2. 递归应该找到递归关系，将问题拆解成更小的相同问题。

### 1.3常见的递归模型：

1. 斐波那契：

函数构造：

```
int fib(int n) {
        if(n==0){
            return 0;
        }
        else if(n==1){
            return 1;
        }
        else{
            return fib(n-1)+fib(n-2);
        }
    }
```

2.阶乘问题：

函数构造：

```
int factorial(int n){
   if(n<=1){
   return 1;
   }
   else{
   return n*factorial(n-1);
   }
}
```

3.汉诺塔模型：

函数构造：

```
void hanoi(int n, char a, char b, char c) {
    if (n == 1) {
        // 终止条件：只有1个盘子时，直接从a移到c
        cout << "将盘子 " << n << " 从 " << a << " 移到 " << c << endl;
        return;
    }
    // 步骤1：将n-1个盘子从a通过c移到b
    hanoi(n - 1, a, c, b);
    // 步骤2：将第n个大盘子从a移到c
    cout << "将盘子 " << n << " 从 " << a << " 移到 " << c << endl;
    // 步骤3：将n-1个盘子从b通过a移到c
    hanoi(n - 1, b, a, c);
}
```

## 2.答疑问题：

### 2.1关于if等语句花括号规范问题：

- 要注意花括号不能少，可以点击其中一个一一对应检查；
- 规范缩进按Tap键；

### 2.2关于内置函数的使用：

要牢记内置函数，可以大大节约编程时间

### 2.3复习传递：

值传递与地址传递：

1. 值传递：函数调用时，实参传给形参  。 形参改变不允许实参。
2. 地址传递：函数构造时可以按&，给定一个地址，从而可以改变其中的值

### 2.4记忆化搜索的应用：

1. 记忆化搜索是指将已经得到的值存储起来，要用的时候可以直接拿来用；

2. 其优点是可以避免冗杂的不必要的重复运算，很好的解决时间限制。

   ![image-20260115232120371](E:\学习心得\关于递归的学习及1.15解疑问题.assets\image-20260115232120371.png)