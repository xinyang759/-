# 1.14学习心得

## 1.二分法排序

### 1.1思想来源：

2^7远远大于100

### 1.2运用要求：

1. 应是数组，链表不行，因为链表不可以随机访问
2. 应是升序或者降序数组

### 1.3函数构造：

```
 int search(vector<int>& nums, int target) {
        int left=0;
        int right=nums.size()-1;
        while(left<=right){
            int mid=(left+right)/2;
            if(nums[mid]<target){
                left=mid+1;
            }
            else if(nums[mid]>target){
                right=mid-1;
            }
            else{
                return mid;
            }
        }
        return -1;
    }
```

## 2.代码错误及修正

![image-20260114204755132](E:\学习心得\错误)

找到错误行，并且在提示中看错误原因，思考纠正。

运用ai豆包分析：![image-20260114210956954](E:\学习心得\豆包分析（递归）)

分析：关键错误解释
递归终止条件错误
原代码：if (l > r) return; → 正确应为 if (l >= r) return;
原因：当 l == r 时，区间只有一个元素，本身就是有序的，无需排序；如果用 l>r，会导致单个元素的区间继续递归，最终出现错误的指针操作。
归并阶段指针和循环逻辑错误
原代码：int i=r,j=mid+1,k=0; + while(i<mid&&j<=r)
错误点：
i 应该从左子数组起点 l 开始，而不是 r；
循环条件应该是 i <= mid && j <= r（左子数组没遍历完且右子数组没遍历完）；
k 应该从 l 开始（对应临时数组的区间起点），而不是 0，避免覆盖其他区间的元素。
临时数组回写错误
原代码：for(int i=1,j=0;i<=r;i++,j++) → 索引完全错误
正确逻辑：从区间起点 l 到终点 r，将临时数组 temp 中对应位置的元素写回原数组 arr，保证只修改当前归并的区间。