# 1.8学习心得（42~63）

## 1.数组

### 1.1数组就是一个集合，里面存储着相同的数据元素

特点：

- 里面的内容数据类型相同
- 存储空间是连续的

### 1.2语法

1. 数据类型 数组名[数组长度]
2. 数据类型 数组名[数组长度]={值1，值2，...}
3. 数据类型 数组名[]={值1，值2，...}

### 1.3一维数组数组名的用途

1. 可以统计整个数组所占的内存长度
2. 可以获取数组在内存中的首地址

### 1.4练习案例

- 五只小猪称重
- 元素逆置
- 冒泡排序

### 1.5二维数组

- 数据类型 数组名 [行数] [列数]
- 数据类型 数组名 [行数] [列数]={{数据1，数据2}，{数据3，数据4}，}
- 数据类型 数组名 [行数] [列数]={数据1，数据2，数据3，数据4，}
- 数据类型 数组名 [] [列数]={数据1，数据2，数据3，数据4，}

### 1.6二维数组名作用

- 可以统计整个数组所占的内存空间
- 获取二维数组首地址

### 1.7案例

- 考试成绩统计

## 2函数

### 2.1作用

封装一段代码，减少复用性

### 语法

`数据类型 函数（参数）{`

`函数体语句；`

`return表达式;`

}`

### 2.2调用函数

- 函数名（参数）

### 2.3值传递

- 函数调用时，实参传给形参
- 形参改变不允许实参

### 2.4四种常见样式

- 有参有返
- 有参无返
- 无返有参
- 无返无参

### 2.5函数的声明

作用：告诉编译器函数及如何调用函数，函数的实际主体可以单独定义

- 函数的声明可以多次使用，但是定义只能一次

### 2.6函数的分文件编写

作用：让代码更加清晰

四步骤：

1. 创建后缀为.h的头文件
2. 创建后缀为.cpp的源文件
3. 在头文件中写函数的声明
4. 在源文件中写函数的定义

## 3.指针

### 3.1指针的定义与使用

作用：可以通过指针间接访问内存

- 内存编写是从0开始的，一般用十六进制表示
- 可以利用指针变量保存地址

语法：

`数据类型 * 变量名`

占用空间4个字节或8个字节，不受数据类型影响

### 3.2空指针与野指针

- 空指针：指向计算机内存中编号为0的空间，用于初始化指针
- 野指针：指向非法的内存空间

注意：皆不可访问

### 3.3const修饰指针

- `const int * p=&a;`:常量指针，可改变地址，不可改变值
- `int * const p=&a;`:指针常量，不可改变地址，可改变值
- `const int * const p=&a;`:不可改变地址，不可改变值

### 3.4指针与数组

作用：可以利用指针访问数组中的元素

### 3.5指针与函数

作用：利用指针可以进行地址传递，进而改变实参的值