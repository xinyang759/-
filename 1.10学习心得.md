# 1.10学习心得

## 1.栈

### 1.1栈的定义

栈是一种先进后出的线性数据结构，栈顶为操作口，栈底封闭。

- 不允许遍历操作

### 1.2c++中的栈---stack容器

一、功能描述：栈容器常用的对外接口。

二、构造函数：

- `stack<T> stk;`  默认构造，创建空栈。
- `stack(const stack &stk);`拷贝构造函数，用已有栈

三、赋值操作：

- ` stack& operator=(const stack &stk);`重载赋值运算符，实现栈的赋值

四、数据存取：

- `push(elem);`向栈顶添加元素
- `pop();`从栈顶移除第一个元素（无返回值）
- `top();`返回栈顶元素（不移除）

五、大小操作：

- `empty();`:判断栈是否为空，返回布尔值
- `size()`:返回栈中元素的个数

## 2.队列

### 2.1队列的定义：

队列是一种先进先出的数据结构，元素从队尾入队，从队头出队，两端分别负责增、删操作。两个出口，一端进入，另一端出。

- 只有队头和队尾才能被访问。不允许遍历。
- 具有单向队列和双向队列，单向仅队尾入、队头出，2端各司其职；双向队头/队尾都能入+出，灵活切换。

### 2.2c++中的队列---queue容器和deque容器

#### 2.21queue容器

一、功能描述：队列容器常用的对外接口，属于单向队列。

二、构造函数：

- `queue<T> que;` ：模板类默认构造，创建空队列
- `queue(const queue &que); `：拷贝构造函数，用已有队列创建新队列

三、赋值操作:

-  `queue& operator=(const queue &que);` ：重载赋值运算符，实现队列的赋值

四、数据存取

- `push(elem); `：向队尾添加元素
- `pop();` ：从队头移除元素（无返回值）
- `back();` ：返回队尾元素（不移除）
- `front(); `：返回队头元素（不移除）

五、大小操作

- `empty(); `：判断队列是否为空，返回布尔值
- `size(); `：返回队列中元素的个数

#### 2.22deque容器

一、功能描述：队列容器常用的对外接口，属于双向队列。

二、构造函数：

- `deque<T> deqT; `：默认构造，创建空的deque容器
- `deque(beg, end); `：将[beg, end)区间内的元素拷贝构造到deque中
- `deque(n, elem);` ：构造包含n个elem元素的deque容器
- `deque(const deque &deq);` ：拷贝构造函数，用已有deque创建新容器

三、赋值算法/接口:

- deque& operator=(const deque &deq); ：重载赋值运算符，实现deque赋值
- assign(beg, end); ：将[beg, end)区间数据拷贝赋值给deque
- assign(n, elem); ：将n个elem元素拷贝赋值给deque

四、大小操作算法/接口:

- `empty();` ：判断deque是否为空，返回布尔值
- `size(); `：返回deque中元素个数
- `resize(num);` ：重新指定deque长度为num，变长则填默认值，变短则删除超出元素
- `resize(num, elem); `：重新指定长度为num，变长则填elem值，变短则删除超出元素

五、插入删除算法/接口:

两端操作

1.  `push_back(elem);` ：在deque尾部添加元素
2.  `push_front(elem); `：在deque头部插入元素
3.  `pop_back(); `：删除deque最后一个元素
4.  `pop_front();` ：删除deque第一个元素

指定位置操作

1.  `insert(pos, elem); `：在pos位置插入elem拷贝，返回新数据位置
2.  `insert(pos, n, elem); `：在pos位置插入n个elem，无返回值
3.  `insert(pos, beg, end);` ：在pos位置插入[beg, end)区间数据，无返回值
4.  `clear(); `：清空deque所有数据
5.  `erase(beg, end); `：删除[beg, end)区间数据，返回下一个数据位置
6.  `erase(pos);` ：删除pos位置数据，返回下一个数据位置

六、数据存取算法/接口:

- `at(int idx); `：返回索引idx对应的元素（越界抛异常）
- `operator[];` ：返回索引idx对应的元素（越界未定义）
- `front(); `：返回deque第一个元素
- `back();` ：返回deque最后一个元素

七、排序算法

-  `sort(iterator beg, iterator end); `：对[beg, end)区间内的deque元素进行排序（需包含 <algorithm> 头文件）

## 3.链表

### 3.1链表的定义

 是一种物理存储单元非连续的存储结构，数据元素的逻辑顺序通过指针链接实现的。链表组成：由一系列结点构成。结点组成：包含存储数据的数据域、存储下一个结点地址的指针域。

### 3.2c++中的链表---list容器

一、功能描述：将数据进行链式储存。STL中的list是双向循环链表，支持头/尾两端的增删操作

二、构造函数

- `list<T> lst; `：模板类默认构造，创建空list

- `list(beg, end);` ：将[beg, end)区间元素拷贝构造到list

- `list(n, elem); `：构造包含n个elem元素的list

- `list(const list &lst);` ：拷贝构造函数，用已有list创建新容器

三、赋值和交换

- `assign(beg, end); `：将[beg, end)区间数据拷贝赋值给list

- `list& operator=(const list &lst);` ：重载赋值运算符，实现list赋值

- `swap(lst); `：交换当前list与参数lst的元素

四、大小操作

- `size();` ：返回list中元素个数

- `empty(); `：判断list是否为空，返回布尔值

- `resize(num); `：重新指定list长度为num，变长填默认值，变短删除超出元素

- `resize(num, elem);` ：重新指定长度为num，变长填elem值，变短删除超出元素

五、插入和删除

两端操作

- `push_back(elem);` ：在list尾部添加元素

- `pop_back();` ：删除list最后一个元素

- `push_front(elem);` ：在list开头插入元素

- `pop_front(); `：删除list第一个元素

指定位置操作

- `insert(pos, elem);` ：在pos位置插入elem拷贝，返回新数据位置

- `insert(pos, n, elem);` ：在pos位置插入n个elem，无返回值

- `insert(pos, beg, end);` ：在pos位置插入[beg, end)区间数据，无返回值

- `clear(); `：清空list所有数据

- `erase(beg, end); `：删除[beg, end)区间数据，返回下一个数据位置

- `erase(pos); `：删除pos位置数据，返回下一个数据位置

- `remove(elem);` ：删除list中所有与elem值匹配的元素

六、数据存取

- `front(); `：返回list第一个元素

- `back();` ：返回list最后一个元素

七、反转和排序

- `reverse(); `：反转list中的元素顺序

- `sort();` ：对list中的元素进行排序