# 双指针学习心得

## 1.理解双指针：

实质就是利用两个变量++或--，从而简化运算过程，减少复杂度；

常见的双指针：

1. 相向双指针；
2. 同向双指针；
3. 快慢双指针；
4. 分离双指针；

### 1.1相向双指针：

相向双指针，就是分别指向数组的两端，然后不断向中间移动。

常用场景：

1. 有序数组的两数/三数/四数之和

核心逻辑：利用数组有序的特性，通过两端指针的移动缩小范围，避免暴力枚举。

- 两数之和（有序数组）：左指针在头，右指针在尾，根据和与目标值的大小调整指针。

- 三数之和：固定一个数，剩余部分用相向双指针找和为0的组合。

- 四数之和：固定两个数，剩余部分用相向双指针找和为目标值的组合。

2. 回文串/回文链表判断

核心逻辑：从两端向中间逐一比较字符/节点值是否相等。

- 回文串：左指针从0开始，右指针从末尾开始，依次比较 s[left] 和 s[right] 。

- 回文链表：先将链表后半部分反转，再用头指针和反转后的后半部分头指针（相向）比较。

3. 有序数组的平方排序

核心逻辑：数组原本按非递减排序，但平方后最大的数一定在两端，用相向双指针从两端向中间收集最大的数。

4. 盛最多水的容器

核心逻辑：左指针在头，右指针在尾，容器面积由“较短的边”和“指针间距”决定，移动较短边的指针尝试增大面积。

5. 字符串的反转

核心逻辑：左指针在头，右指针在尾，交换两端字符后向中间移动。

6. 移除有序数组中的重复元素（拓展）

核心逻辑：左指针记录新数组的末尾，右指针从末尾向中间移动，跳过重复元素（适用于需要保留唯一元素的场景）。

关键特点：

- 适用结构：有序数组/字符串（核心是两端信息可比较）；

- 时间复杂度：通常为O(n)，远优于暴力法的O(n²)；

- 空间复杂度：通常为O(1)（除了需要额外存储结果的场景）。

### 1.2同向双指针：

同向双指针就是两个指针在同一端出发，进行移动，可维护一个窗口；

1. 数组的筛选/去重（原地修改）

核心逻辑：慢指针记录“新数组”的末尾，快指针遍历原数组，将符合条件的元素移动到慢指针位置。

- 移除元素：快指针找不等于目标值的元素，慢指针记录新数组的位置。

- 移除重复元素：快指针遍历，慢指针保留唯一元素的最后一个位置。

- 移动零：快指针找非零元素，与慢指针位置交换，将零移到末尾。

2. 链表的“环/中点/倒数节点”问题

核心逻辑：快慢指针以不同速度移动（快指针速度通常是慢指针的2倍），利用速度差实现目标。

- 链表的中间节点：快指针走2步，慢指针走1步，快指针到尾时慢指针在中点。

- 链表中是否有环：快指针走2步，慢指针走1步，若相遇则有环。

- 删除链表的倒数第N个节点：快指针先出发N步，再与慢指针同速移动，快指针到尾时慢指针在倒数第N+1个节点。

3. 滑动窗口（子数组/子串问题）

核心逻辑：快指针扩展窗口，慢指针收缩窗口，维护窗口内的合法区间（属于同向双指针的拓展）。

- 无重复字符的最长子串：快指针扩展窗口，出现重复时慢指针收缩窗口。

- 长度最小的子数组：快指针扩展窗口直到满足条件，慢指针收缩窗口找最小长度。

- 字符串的排列：用窗口维护字符频率，快指针扩展、慢指针收缩判断是否匹配。

4. 有序数组的合并

核心逻辑：两个指针分别遍历两个有序数组，将较小元素放入结果（也可用于链表合并）。

- 合并两个有序数组：从数组末尾开始，用双指针将较大元素放入最终位置。

- 合并两个有序链表：用哑节点+双指针拼接较小节点。

关键特点

- 适用结构：数组、链表（无需有序，但滑动窗口等场景常结合有序性）；

- 时间复杂度：通常为O(n)，避免嵌套循环；

- 空间复杂度：通常为O(1)（原地修改场景）或O(n)（结果存储场景）。

### 1.3快慢指针：

快慢指针就是一个指针移动快，另一个移动慢，利用速度差实现目标，主要解决链表问题

应用场景：

1. 找链表的中间节点

- 适用场景：需要找到链表中点，有两个中点时返回第二个。

- 逻辑步骤：

1. 快慢指针都从链表头开始；

2. 快指针每次走2步，慢指针每次走1步；

3. 当快指针走到链表末尾（要么指向最后一个节点，要么指向空），慢指针的位置就是链表的中间节点。

- 举例：链表 1→2→3→4→5 ，快指针走到5时，慢指针在3；链表 1→2→3→4→5→6 ，快指针走到空时，慢指针在4。

2. 判断链表是否有环

- 适用场景：判断链表是否形成闭环（比如某个节点的next指向前面的节点）。

- 逻辑步骤：

1. 快慢指针都从链表头开始；

2. 快指针走2步，慢指针走1步；

3. 如果链表有环，快指针会在环里绕圈并最终追上慢指针（相遇）；如果没环，快指针会先走到链表末尾。

- 核心逻辑：就像龟兔赛跑，兔子（快指针）跑的快，只要赛道有环，兔子一定会追上乌龟（慢指针）。

3. 找链表环的入口节点

- 适用场景：已知链表有环，找到环开始的那个节点。

- 逻辑步骤：

1. 先按“判断环”的方法，让快慢指针相遇，确定相遇点；

2. 把慢指针放回链表头，快指针留在相遇点；

3. 此时快慢指针都改走1步，它们再次相遇的位置，就是环的入口节点。

- 核心逻辑：相遇点到环入口的距离，等于链表头到环入口的距离。

4. 删除链表的倒数第N个节点

- 适用场景：删除链表中倒数第N个节点，避免先遍历算长度再删除的两次遍历。

- 逻辑步骤：

1. 先创建一个哑节点（哨兵），快慢指针都从哑节点开始（解决删除头节点的边界问题）；

2. 让快指针先向前走N步；

3. 快慢指针再一起向前走，直到快指针走到链表末尾；

4. 此时慢指针指向倒数第N+1个节点，让慢指针跳过倒数第N个节点，就完成了删除。

- 核心逻辑：快指针先跳N步，让快慢指针之间形成N个节点的“距离差”，快指针到尾时，慢指针正好定位到要删除节点的前一个。

 

快慢指针的核心共性

1. 速度差：是实现所有功能的关键，要么快指针走2步、慢指针走1步，要么快指针先跳N步再同速走；

2. 空间省：只需要两个指针，不用额外存储数据；

3. 效率高：只遍历链表一次，时间效率是最优的。

### 1.4分离指针：

分离指针就是两个指针分别处于不同的容器中，各自按规则移动，根据业务需求完成数据的整合/匹配，利用有序性让指针移动更高效。

应用场景：

核心场景1：合并两个有序数组/链表

这是分离指针最经典的用法，也是你之前做过的「合并两个有序链表」「合并两个有序数组」的核心思路。

逻辑步骤（以合并为例）：

1. 指针 p1 指向第一个有序容器的头，指针 p2 指向第二个有序容器的头；

2. 比较两个指针指向的元素，把较小的元素加入结果，同时移动对应指针；

3. 重复步骤2，直到其中一个指针走到容器末尾；

4. 把另一个容器中剩余的元素直接追加到结果末尾。

举例：

- 链表1： 1→2→4 ，链表2： 1→3→4 

-  p1 先指1， p2 先指1 → 选1， p2 后移；

-  p1 指1， p2 指3 → 选1， p1 后移；

- 依次类推，最终合并出 1→1→2→3→4→4 。

核心场景2：两个数组的元素匹配/交集查找

适用于找两个有序数组的交集、并集、差集，比如LeetCode 350（两个数组的交集 II）。

逻辑步骤（以找交集为例）：

1. 指针 p1 、 p2 分别指向两个有序数组的起始位置；

2. 若 p1 指向的元素 ==  p2 指向的元素 → 加入交集，两个指针都后移；

3. 若 p1 指向的元素 <  p2 指向的元素 →  p1 后移（找更大的元素匹配）；

4. 若 p1 指向的元素 >  p2 指向的元素 →  p2 后移（找更大的元素匹配）；

5. 直到其中一个指针走到数组末尾，结束。

举例：

- 数组1： [1,2,2,3] ，数组2： [2,2,4] 

- 最终交集为 [2,2] 。

核心场景3：两个链表的相交节点查找

适用于找两个无环链表的第一个相交节点，这里的分离指针是交替遍历两个链表的特殊形式：

逻辑步骤：

1. 指针 p1 从链表A头出发，指针 p2 从链表B头出发；

2.  p1 遍历完A后，跳到B的头继续遍历； p2 遍历完B后，跳到A的头继续遍历；

3. 两个指针最终会在相交节点相遇（若相交），或同时走到末尾（若不相交）；

- 核心逻辑：两个指针走的总路程相同，必然会在相交点相遇。

 

分离指针的核心共性

1. 指针分离：不在同一个容器，各自遍历独立的数组/链表；

2. 依赖有序性：绝大多数场景下，两个容器都是有序的，才能通过指针移动高效完成操作；

3. 时间效率：O(m+n)（m、n为两个容器的长度），两个指针各遍历一次，无嵌套循环；

4. 空间效率：通常为O(1)（原地操作）或O(m+n)（存储结果）。

## 2.答疑问题及总结：

### 回文题型：

回文可采用相向指针向中间移动进行判断；

今日问题所在，没有注意while的循环条件，导致时间限制。并且应该对while嵌套中的关系严谨

```
bool isPalindrome(string s) {
        int l=0;
        int r=s.size()-1;
        while(l<r){
            while(l<r&&!zimu(s[l])){
                l++;
                }
            while(l<r&&!zimu(s[r])){
                r--;
                }
            if(change(s[l])!=change(s[r])){
                return false;
            }
            else{
                l++;
                r--;
            }
        }
        return true;
```

