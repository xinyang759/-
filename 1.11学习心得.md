# 1.11学习心得

## 1.set（multiset）容器

简介：

- 所有的元素插入时会自动排序。

本质：

- 属于关联式容器，底层结构是二叉树实现。

set和multiset的区别：

前者不可插入重复元素，后者可以。

1. 构造与赋值
- 默认构造： `set<T> st; `
- 拷贝构造： `set(const set &st);` 
- 赋值重载： `set& operator=(const set &st);` 
2. 大小与交换
-  `size() `：返回元素个数
-  `empty()` ：判断容器是否为空
-  `swap(st) `：交换两个set容器
3. 插入与删除
-  `insert(elem) `：插入元素
-  `clear() `：清除所有元素
-  `erase(pos)` ：删除迭代器 pos 指向的元素，返回下一个元素迭代器
-  `erase(beg, end)` ：删除 [beg,end) 区间元素，返回下一个元素迭代器
-  `erase(elem) `：删除值为 elem 的元素
4. 查找与统计
-  `find(key) `：查找 key ，存在则返回对应迭代器，否则返回 set.end() 
-  `count(key) `：统计 key 的元素个数（set中仅0或1）

## 2.pair队组

功能描述：

- 成对出现的数据，利用队组可以返回两个数据

创建方式：

- `pair<type,type>p(value1,value2);`
- `pair<type,type>make_pair(value1,value2);`

## 3.map/multimap容器

简介

- map元素为pair类型，第一个元素是key（索引作用），第二个是value（实值）。
- 元素会按key值自动排序。

本质

- 属于关联式容器，底层由二叉树实现。

优点

- 可通过key值快速查找对应的value值。

map与multimap区别

- map：不允许重复key值。
- multimap：允许重复key值。

1. 构造与赋值
- 默认构造：` map<T1, T2> mp; `
- 拷贝构造： `map(const map &mp); `
- 赋值重载： `map& operator=(const map &mp); `
2. 大小与交换
-  `size()` ：返回元素
-  `empty() `：判断容器是否为空

-  `swap(st) `：交换两个map容器

3. 插入与删除

-  `insert(elem)`：插入键值对元素

-  `clear() `：清除所有元素

-  `erase(pos)` ：删除迭代器 pos 指向的元素，返回下一个元素迭代器

-  `erase(beg, end)` ：删除 [beg,end) 区间元素，返回下一个元素迭代器

-  `erase(key) `：删除键为 key 的元素

4. 查找与统计

-  `find(key)` ：查找 key ，存在则返回对应迭代器，否则返回 map.end() 

-  `count(key) `：统计 key 的元素个数（map中仅0或1）